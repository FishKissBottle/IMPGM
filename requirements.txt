# IMPGM validated runtime dependencies
# Validated with Python 3.11 and the package versions recorded in XXX.txt.
#
# Install the PyTorch build for the target accelerator first. The validated
# CUDA environment uses torch 2.11.0+cu128 and torchvision 0.26.0+cu128;
# Intel XPU and CPU environments should use the matching official build.
torch==2.11.0
torchvision==0.26.0

# Core numerical and image processing
numpy==1.26.4
scipy==1.17.1
Pillow==11.3.0
matplotlib==3.11.1
opencv-python-headless==4.10.0.84
albumentations==2.0.8
einops==0.8.2

# Geospatial I/O
# Install GDAL 3.8.4 separately from a compatible local wheel; see README.md.
rasterio==1.4.4

# Configuration, progress reporting, and training logs
PyYAML==6.0.3
tqdm==4.70.0
tensorboard==2.21.0

# Quality metrics and feature extractors
piqa==1.3.2
lpips==0.1.4
transformers==4.57.6
huggingface-hub==0.36.2
safetensors==0.8.0

# Comparison models
diffusers==0.30.3
accelerate==1.14.0
datasets==2.21.0
dill==0.3.8
ftfy==6.3.1
Jinja2==3.1.6
packaging==25.0
requests==2.32.5
scikit-image==0.26.0

# Notebook, UI, build-tool, and CUDA runtime packages shown in XXX.txt are
# transitive or environment-specific dependencies and are intentionally not
# duplicated here.
